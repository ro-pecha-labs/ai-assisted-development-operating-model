schema: om.active-state/v1

objective:
  id: QREC-01
  summary: Recover the current trusted development objective from the minimal OM2 bootstrap.

trusted_baseline:
  ref: baseline/qrec-accepted
  commit: 0123456789abcdef0123456789abcdef01234567

active_work:
  issue: 42
  branch: feature/recovery-target
  pull_request: 43

phase: development

blockers: []

next_actions:
  - Inspect the referenced live branch, issue, pull request, and required checks.
  - Continue the bounded development objective if live state is consistent.

conditional_reads:
  - when: architecture_question
    ref: docs/architecture/
