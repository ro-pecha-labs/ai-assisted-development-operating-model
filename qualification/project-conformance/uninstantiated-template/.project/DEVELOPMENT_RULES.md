# Project rules placeholder for OM template-instantiation negative qualification.

This file exists only so the negative case fails specifically on the uninstantiated Operating Model ref/commit sentinels rather than on a missing project-rules locator.
